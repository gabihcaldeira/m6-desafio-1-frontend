import styled from "styled-components";

export const Error = styled.span`
  font-size: 11px;
  font-weight: 600;
  line-height: 13px;

  color: var(--color-error-text);

  position: absolute;
  margin-top: 5px;
`;
